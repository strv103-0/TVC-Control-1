# 제어기 시뮬레이션

실기체에 올리기 전에 PID 게인과 짐벌 한계를 확인하는 곳입니다.

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

`.venv/`는 git에서 제외되므로 PC마다 위 명령을 한 번씩 실행하면 됩니다.
